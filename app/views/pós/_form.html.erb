<%= form_with(model: pó, local: true) do |form| %>
  <% if pó.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pó.errors.count, "error") %> prohibited this pó from being saved:</h2>

      <ul>
        <% pó.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
